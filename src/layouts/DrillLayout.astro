---
import BaseLayout from "./BaseLayout.astro";
const { title, summary, meta } = Astro.props;
---
<BaseLayout title={`${title} — Hockey Drills`}>
  <div class="card">
    <h1 class="h1">{title}</h1>
    {summary ? <p class="sub">{summary}</p> : null}

    {meta ? (
      <>
        <hr class="sep" />
        <div class="meta">
          {meta.map((m) => <span class="pill">{m}</span>)}
        </div>
      </>
    ) : null}
  </div>

  <div style="height: 12px;"></div>

  <div class="card prose">
    <slot />
  </div>
</BaseLayout>
